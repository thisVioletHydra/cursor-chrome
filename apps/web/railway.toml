[build]
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @cursor-chrome/web build"

[deploy]
startCommand = "node apps/web/build/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
