{
  "name": "@cursor-chrome/extension",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsdown && node scripts/copy-static.mjs"
  },
  "dependencies": {
    "@cursor-chrome/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "tsdown": "^0.16.8",
    "typescript": "^5.9.3"
  }
}
